%section.login-form.password
  = form_tag password_path, method: :post do
    %h2= t('.title')
    .form-field
      = label_tag :email
    = email_field_tag :email, nil, autocomplete: :off
    .form-buttons
      = submit_tag t('.submit'), class: 'primary'
